import { Card, CardContent, CardHeader, CardTitle } from "../ui/card";
import { Badge } from "../ui/badge";
import { BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, PieChart, Pie, Cell, LineChart, Line } from "recharts";
import { Calendar, Building2, Car, AlertTriangle, TrendingUp, Users, Clock } from "lucide-react";

// 醫낅ぉ蹂??쒖꽕 ?댁슜瑜??곗씠??
const sportUsageData = [
  { sport: "異뺢뎄", usage: 85 },
  { sport: "?띻뎄", usage: 72 },
  { sport: "諛곌뎄", usage: 68 },
  { sport: "?섏쁺", usage: 90 },
  { sport: "?≪긽", usage: 76 },
  { sport: "?뚮땲??, usage: 63 }
];

// ?쒖꽕蹂??덉빟瑜??곗씠??
const facilityReservationData = [
  { name: "?덈젴愿", value: 45, color: "#005BAC" },
  { name: "泥댁쑁愿", value: 30, color: "#28A745" },
  { name: "?뚯쓽??, value: 15, color: "#17A2B8" },
  { name: "?숈냼", value: 10, color: "#FFC107" }
];

// ?쇰퀎 諛곗감 ?붿껌 ?곗씠??
const dailyDispatchData = [
  { date: "12/15", requests: 12 },
  { date: "12/16", requests: 15 },
  { date: "12/17", requests: 18 },
  { date: "12/18", requests: 14 },
  { date: "12/19", requests: 20 },
  { date: "12/20", requests: 16 },
  { date: "12/21", requests: 22 }
];

// 李⑤웾蹂??댁슜瑜??곗씠??
const vehicleUsageData = [
  { vehicle: "??뺣쾭??, usage: 78 },
  { vehicle: "以묓삎踰꾩뒪", usage: 65 },
  { vehicle: "?뱀슜李?, usage: 82 },
  { vehicle: "?뱁빀李?, usage: 71 },
  { vehicle: "?뱀닔李⑤웾", usage: 45 }
];

const todayStats = [
  {
    title: "?ㅻ뒛 珥??덉빟",
    value: "47",
    subtitle: "?덈젴?쒖꽕 32, 遺??쒖꽕 15",
    icon: Calendar,
    color: "text-blue-600"
  },
  {
    title: "?꾩옱 媛?숇쪧",
    value: "73%",
    subtitle: "?꾩씪 ?鍮?+5%",
    icon: TrendingUp,
    color: "text-green-600"
  },
  {
    title: "李⑤웾 諛곗감",
    value: "16",
    subtitle: "?뱀씤 12, ?湲?4",
    icon: Car,
    color: "text-purple-600"
  },
  {
    title: "?쒖꽕 ?댁슜??,
    value: "284",
    subtitle: "?좎닔 198, ?ㅽ깭??86",
    icon: Users,
    color: "text-orange-600"
  }
];

const alerts = [
  { id: 1, type: "warning", message: "泥댁쑁愿 B ?먯뼱而??먭? ?꾩슂", time: "10遺??? },
  { id: 2, type: "info", message: "?덈줈??諛곗감 ?붿껌 3嫄?, time: "15遺??? },
  { id: 3, type: "error", message: "?섏쁺??A ?덉빟 珥덇낵", time: "25遺??? },
  { id: 4, type: "warning", message: "?뚯쓽??1 ?꾨줈?앺꽣 怨좎옣", time: "1?쒓컙 ?? }
];

export function StatsCharts() {
  return (
    <div className="space-y-6">
      {/* ?ㅼ떆媛??덉빟 ?꾪솴 */}
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        {todayStats.map((stat) => {
          const Icon = stat.icon;
          return (
            <Card key={stat.title}>
              <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-2">
                <CardTitle className="text-sm font-medium text-muted-foreground">
                  {stat.title}
                </CardTitle>
                <Icon className={`h-4 w-4 ${stat.color}`} />
              </CardHeader>
              <CardContent>
                <div className="text-2xl font-bold">{stat.value}</div>
                <p className="text-xs text-muted-foreground mt-1">
                  {stat.subtitle}
                </p>
              </CardContent>
            </Card>
          );
        })}
      </div>

      <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
        {/* 醫낅ぉ蹂??쒖꽕 ?댁슜瑜?*/}
        <Card>
          <CardHeader>
            <CardTitle>醫낅ぉ蹂??쒖꽕 ?댁슜瑜?/CardTitle>
          </CardHeader>
          <CardContent>
            <ResponsiveContainer width="100%" height={300}>
              <BarChart data={sportUsageData}>
                <CartesianGrid strokeDasharray="3 3" />
                <XAxis dataKey="sport" />
                <YAxis />
                <Tooltip formatter={(value) => [`${value}%`, "?댁슜瑜?]} />
                <Bar dataKey="usage" fill="#005BAC" />
              </BarChart>
            </ResponsiveContainer>
          </CardContent>
        </Card>

        {/* ?쒖꽕蹂??덉빟瑜?*/}
        <Card>
          <CardHeader>
            <CardTitle>?쒖꽕蹂??덉빟瑜?/CardTitle>
          </CardHeader>
          <CardContent>
            <ResponsiveContainer width="100%" height={300}>
              <PieChart>
                <Pie
                  data={facilityReservationData}
                  cx="50%"
                  cy="50%"
                  innerRadius={60}
                  outerRadius={120}
                  paddingAngle={5}
                  dataKey="value"
                >
                  {facilityReservationData.map((entry, index) => (
                    <Cell key={`cell-${index}`} fill={entry.color} />
                  ))}
                </Pie>
                <Tooltip formatter={(value) => [`${value}%`, "?덉빟瑜?]} />
              </PieChart>
            </ResponsiveContainer>
            <div className="flex justify-center space-x-4 mt-4">
              {facilityReservationData.map((entry) => (
                <div key={entry.name} className="flex items-center space-x-2">
                  <div 
                    className="w-3 h-3 rounded-full" 
                    style={{ backgroundColor: entry.color }}
                  ></div>
                  <span className="text-sm">{entry.name} {entry.value}%</span>
                </div>
              ))}
            </div>
          </CardContent>
        </Card>
      </div>

      <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
        {/* ?쇰퀎 諛곗감 ?붿껌 嫄댁닔 */}
        <Card>
          <CardHeader>
            <CardTitle>?쇰퀎 諛곗감 ?붿껌 嫄댁닔</CardTitle>
          </CardHeader>
          <CardContent>
            <ResponsiveContainer width="100%" height={300}>
              <LineChart data={dailyDispatchData}>
                <CartesianGrid strokeDasharray="3 3" />
                <XAxis dataKey="date" />
                <YAxis />
                <Tooltip formatter={(value) => [`${value}嫄?, "?붿껌 嫄댁닔"]} />
                <Line 
                  type="monotone" 
                  dataKey="requests" 
                  stroke="#28A745" 
                  strokeWidth={3}
                  dot={{ fill: "#28A745", strokeWidth: 2, r: 6 }}
                />
              </LineChart>
            </ResponsiveContainer>
          </CardContent>
        </Card>

        {/* 李⑤웾蹂??댁슜瑜?*/}
        <Card>
          <CardHeader>
            <CardTitle>李⑤웾蹂??댁슜瑜?/CardTitle>
          </CardHeader>
          <CardContent>
            <ResponsiveContainer width="100%" height={300}>
              <BarChart data={vehicleUsageData} layout="horizontal">
                <CartesianGrid strokeDasharray="3 3" />
                <XAxis type="number" />
                <YAxis dataKey="vehicle" type="category" width={80} />
                <Tooltip formatter={(value) => [`${value}%`, "?댁슜瑜?]} />
                <Bar dataKey="usage" fill="#17A2B8" />
              </BarChart>
            </ResponsiveContainer>
          </CardContent>
        </Card>
      </div>

      {/* 寃쎄퀬 諛??뚮┝ */}
      <Card>
        <CardHeader>
          <CardTitle className="flex items-center gap-2">
            <AlertTriangle className="h-5 w-5 text-orange-500" />
            寃쎄퀬 諛??뚮┝
          </CardTitle>
        </CardHeader>
        <CardContent>
          <div className="space-y-3">
            {alerts.map((alert) => (
              <div key={alert.id} className="flex items-center justify-between p-3 border rounded-lg">
                <div className="flex items-center gap-3">
                  <div className={`w-2 h-2 rounded-full ${
                    alert.type === "error" ? "bg-red-500" :
                    alert.type === "warning" ? "bg-yellow-500" : "bg-blue-500"
                  }`}></div>
                  <span>{alert.message}</span>
                </div>
                <div className="flex items-center gap-2">
                  <span className="text-sm text-muted-foreground">{alert.time}</span>
                  <Badge 
                    variant="secondary"
                    className={
                      alert.type === "error" ? "bg-red-100 text-red-800" :
                      alert.type === "warning" ? "bg-yellow-100 text-yellow-800" : 
                      "bg-blue-100 text-blue-800"
                    }
                  >
                    {alert.type === "error" ? "?ㅻ쪟" :
                     alert.type === "warning" ? "寃쎄퀬" : "?뺣낫"}
                  </Badge>
                </div>
              </div>
            ))}
          </div>
          
          <div className="mt-6 p-4 bg-primary/5 rounded-lg">
            <div className="flex items-center justify-between">
              <div>
                <h4 className="font-medium">?뱀씤 ?湲?嫄댁닔</h4>
                <p className="text-sm text-muted-foreground">?쒖꽕 ?덉빟 7嫄? 李⑤웾 諛곗감 4嫄댁씠 ?뱀씤 ?湲?以묒엯?덈떎</p>
              </div>
              <Badge className="bg-primary text-primary-foreground">
                11嫄??湲?
              </Badge>
            </div>
          </div>
        </CardContent>
      </Card>
    </div>
  );
}
